server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/antigone_rh
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  sql:
    init:
      mode: always

  mail:
    host: smtp.gmail.com
    port: 587
    username: yosr.khriji27@gmail.com
    password: fbgodopejvihkutw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    test-connection: false

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  mail:
    from: yosr.khriji27@gmail.com
    enabled: true
  upload:
    dir: uploads/justificatifs
